---
title: "5.5.5. im2col::w , im2col_no_offs::w and im2col::w::128 modes"
section: "5.5.5"
version: "9.4"
url: https://docs.nvidia.com/cuda/parallel-thread-execution/index.html#tensor-im2col-w-w128-modes
---

<a id="tensor-im2col-w-w128-modes"></a>

<a id="id83"></a>

###  5.5.5. `im2col::w`, `im2col_no_offs::w` and `im2col::w::128` modes

These modes are similar to the im2col mode with the restriction that elements are accessed across the `W` dimension only while keeping the `H` and `D` dimension constant.

All the constraints and rules of the im2col mode apply to these modes as well. Note that for these modes, the legal [Swizzling Modes](5.5.7-swizzling-modes.md#tensor-swizzling-modes) is required. In other words, swizzling mode must not be set to (i) no swizzle and (ii) 128-byte swizzle mode with 32-byte atomicity with 8-byte flip.

The number of elements accessed in the `im2col::w::128` mode is fixed and is equal to 128. The number of elements accessed in the `im2col::w` and `im2col_no_offs::w` modes depend on the Pixels-per-Column field in the TensorMap.

Subsections:

- [5.5.5.1. Bounding Box](5.5.5.1-bounding-box.md)
- [5.5.5.2. Traversal Stride](5.5.5.2-traversal-stride.md)
- [5.5.5.3. wHalo](5.5.5.3-whalo.md)
- [5.5.5.4. wOffset](5.5.5.4-woffset.md)
